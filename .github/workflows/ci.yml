  name: CI flow 
  
  on:
    push:
      branches: [ main ]
    pull_request: 
      types: [opened, synchronize]
  
  jobs:
    build: 
      runs-on: ubuntu-latest

      steps: 
      - name: Checkout code 
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha }}

      # - name: Build Docker image
      #   run: docker build -t criminal-map -f Dockerfile-live .

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Cache Docker layers
        uses: actions/cache@v3
        with:
          path: /tmp/.buildx-cache
          key: buildx-${{ github.sha }}
          restore-keys: |
            buildx-

      - name: Build Docker image with cache
        uses: docker/build-push-action@v5
        with:
          context: .
          file: Dockerfile-live
          tags: criminal-map
          cache-from: type=local,src=/tmp/.buildx-cache
          cache-to: type=local,dest=/tmp/.buildx-cache

      - name: Log in to DockerHub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Publish image into Docker Hub
        uses: docker/build-push-action@v5
        with:
          context: .
          file: Dockerfile-live
          push: true
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/criminal-map:${{ github.run_number }}

      # - name: Trigger Vercel Deploy
      #   run: |
      #     curl -X POST https://api.vercel.com/v1/integrations/deploy/${{secrets.VERCEL_DEPLOY_HOOK}}